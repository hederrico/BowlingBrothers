// Chart.js placeholder - CDN blocked
window.Chart = function(ctx, config) {
    this.canvas = ctx.canvas || ctx;
    this.config = config;
    this.destroy = function() {};
    this.resize = function() {};
    this.toDataURL = function() { return 'data:image/png;base64,'; };
};
Chart.register = function() {};
Chart.defaults = { 
    plugins: { 
        legend: { labels: {} }, 
        tooltip: {} 
    }, 
    scales: { 
        x: { ticks: {}, grid: {}, border: {} }, 
        y: { ticks: {}, grid: {}, border: {} } 
    } 
};
